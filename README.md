# Omnifocus Tag Converter

Simply finds any task that has a note with '#tag' convention, and does a fuzzy match against existing contexts, adding the first found context to the task.

Runs with no options.
